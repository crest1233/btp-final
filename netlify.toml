[build]
command = "npm run build"
publish = "build"

[build.environment]
# Keep the build stable and compatible
NODE_VERSION = "18"
# Frontend will call your Render backend
VITE_API_BASE_URL = "https://inverso-api.onrender.com"